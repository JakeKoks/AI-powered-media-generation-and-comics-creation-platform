# Raspberry Pi Environment Configuration
# Copy to .env.pi and fill in your values

# ==============================================
# 🤖 RASPBERRY PI CONFIGURATION
# ==============================================

# 🌐 Domain & Network
DOMAIN=jakekoks.fun
PI_IP=192.168.1.50
PC_IP=192.168.1.100

# 💾 Database Configuration
DB_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD
POSTGRES_DB=ai_comics_db
POSTGRES_USER=comics_user
POSTGRES_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD

# 🗄️ Redis Configuration  
REDIS_PASSWORD=CHANGE_THIS_TO_SECURE_REDIS_PASSWORD

# 🔐 JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=CHANGE_THIS_TO_RANDOM_JWT_SECRET

# 📁 MinIO Object Storage
MINIO_ACCESS_KEY=CHANGE_THIS_TO_MINIO_ACCESS_KEY
MINIO_SECRET_KEY=CHANGE_THIS_TO_SECURE_MINIO_SECRET

# 📊 Grafana Admin
GRAFANA_PASSWORD=CHANGE_THIS_TO_SECURE_GRAFANA_PASSWORD

# 🤖 AI Worker Configuration
AI_WORKER_URL=http://192.168.1.100:8080
AI_WORKER_HEALTH_CHECK_INTERVAL=30

# 🚀 Application Settings
NODE_ENV=production
API_URL=http://192.168.1.50:3001
FRONTEND_URL=http://192.168.1.50:3000

# 📊 Monitoring
PROMETHEUS_RETENTION=30d
GRAFANA_ALLOW_SIGNUP=false

# ==============================================
# 🛠️ GENERATE SECURE PASSWORDS:
# ==============================================
# 
# For DB_PASSWORD:     openssl rand -base64 32
# For REDIS_PASSWORD:  openssl rand -base64 24  
# For JWT_SECRET:      openssl rand -base64 32
# For MINIO keys:      openssl rand -base64 20
# For GRAFANA_PASSWORD: Use a strong password manager
#
# ==============================================
